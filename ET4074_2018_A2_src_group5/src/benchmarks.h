
#ifndef _BENCHMARKS_H_
#define _BENCHMARKS_H_

// Runs the pocsag benchmark.
int main_pocsag(void);

// Runs the matrix benchmark.
int main_matrix(void);

// Runs the bcnt benchmark.
int main_bcnt(void);

// Runs the fir benchmark.
int main_fir(void);

#endif
